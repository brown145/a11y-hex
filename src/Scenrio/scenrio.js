import makeScenrio from "./makeScenrio";

const scenrioJSON = {
  title: "tmp",
  description: "tmp",
  orientation: "pointy",
  width: 5,
  height: 5,
  source: { x: 1, y: 1 },
};

export default makeScenrio(scenrioJSON);
